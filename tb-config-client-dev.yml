spring:
  application:
    name: tb-feign
  redis:
    database: ${my.reids}
    host: localhost
    port: 6379
    timeout: 6000ms
    jedis:
      pool:
        max-active: 200
        max-wait: -1ms
        max-idle: 10
        min-idle: 0